export const Address = [
    "Address" 
]


export const AddressDetails = (data) => {
    debugger
    
    var AddressDetails = [
       
        [`${data.Customer}`], 
        [`${data.CustomerAddress}`]  ,
        [`${data.Contact}`],
        [`${data.Email}`],
        [`${data.ReceiptDate}`],

    ]
    return AddressDetails;
} 


